Array can store data of specific data type.
Elements of array are located in a contiguous i.e. there is no gap between them.
Each element of array has a unique index.
The size of array is predefined and cannot be modified.

why need array? --> used to store any data